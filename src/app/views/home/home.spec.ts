import { HomeComponent } from './home.component';

describe('home: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new HomeComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
