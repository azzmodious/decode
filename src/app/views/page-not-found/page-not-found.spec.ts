import { PageNotFoundComponent } from './page-not-found.component';

describe('pageNotFound: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new PageNotFoundComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
